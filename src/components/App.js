import React from "react";
import Connect from "./Connect";

const App = () => {
    return (
        <div className="ui container">
            <Connect />
        </div>
    );
};

export default App;
